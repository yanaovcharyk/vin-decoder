import { Layout } from "@/common/components";
import { VinDecoder } from "@/decoder/components";

export const DecoderPage = () => {
  return (
    <Layout>
      <VinDecoder />
    </Layout>
  );
};
