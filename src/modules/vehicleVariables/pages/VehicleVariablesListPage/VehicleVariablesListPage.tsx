import { Layout } from "@/common/components";
import { VehicleVariablesList } from "@/vehicleVariables/components";

export const VehicleVariablesListPage = () => {
  return (
    <Layout>
      <VehicleVariablesList />
    </Layout>
  );
};
